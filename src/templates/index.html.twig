{% include 'header.html.twig' %}
<h1>Strona główna</h1>
{% for post in posts %}
<div class='title'></div><h1>Tytuł: 
    <h1 style="display: inline;">{{ post.getTitle() }}</h1>
    {{ post.getTimestamp() }}
</h1></div>

<div class="post">
    <img class="img" src="{{ post.getFilename() }}" style="width: 300px; height: 400px;"><br>
</div>

{% endfor %}
{% include 'footer.html.twig' %}
